num = int(input('Enter number: '))
u = num // 1 % 10
d = num // 10 % 10
c = num // 100 % 10
m = num // 1000 % 10
print('The unit:{}'.format(u))
print('The ten: {}'.format(d))
print('The hundred: {}'.format(c))
print('The thousand: {}'.format(m))
