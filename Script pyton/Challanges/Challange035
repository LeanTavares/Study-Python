#triangle analysis
s1 = float(input('enter a segment:'))
s2 = float(input('enter a segment:'))
s3 = float(input('enter a segment'))

if s1 < s2 + s3 and s2 < s1 + s3 and s3 < s1 + s2:
    print('is a triangle!')
