#the first term and the ratio of a PA. At the end, show the first 10 terms of this progression (whith while).

f = int(input('Enter a first term of PA: '))
r = int(input('Enter a ratio of PA: '))

t = f
cont = 1
while cont <= 10:
    print('{} >'.format(t),end='')
    t += r
    cont += 1
print('end')

