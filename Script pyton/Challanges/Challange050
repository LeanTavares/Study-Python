#read six whole numbers and show the sum of only those that are even. If the value entered is odd, disregard it.

sum = 0
for i in range(0, 7):
    n = int(input('Enter a number: '))
    if n % 2 == 0:
        sum += n
print('The sum of even numbers is: {}'.format(sum))